# Launch assets

This directory reserves the README demo slot for a short, captioned cold-recall GIF. The planned asset name is `aoms-cold-recall.gif`; it is not included yet.

The final capture should show setup, one user-authored workspace memory, a fully restarted host session, scoped recall, and the resulting receipt ID. It must use synthetic data and must not imply evidence beyond the repository's **REHEARSAL**-grade relay artifacts.
