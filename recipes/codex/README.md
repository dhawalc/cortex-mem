# Codex: automatic memory instructions

Merge `config.toml` into `~/.codex/config.toml`, replacing the workspace
placeholder with the absolute project path. Append `AGENTS.md.snippet` to the
project's `AGENTS.md`. The MCP block makes memory callable; the instructions make
recall the default session-start behavior and define selective capture.
